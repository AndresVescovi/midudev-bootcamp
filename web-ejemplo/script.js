const list = []
const anotherList = list.concat(157)

const persona = {
    name : 'andres',
    twiter: '@andres',
    age: 18,
    isDev: false,
    liks: ['https://midu.tube','https://midu.tube']
}

const sumar = (operando1, operando2) =>{
    console.log(operando1)
    console.log(operando2)
    return operando1 + operando2
}

function restar (a, b){
    return a - b
}

op1 = 2
op2 = 2

console.log(sumar(op1, op2))
